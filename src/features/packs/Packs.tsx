import React, { FC } from 'react'
import { Root } from 'routes/root'

type PropsType = {}

export const Packs: FC<PropsType> = ({}) => {
    return (
        <div>
            <h1>Packs</h1>
        </div>
    )
}
