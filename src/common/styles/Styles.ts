import styled from '@emotion/styled'

/**
 * Circular - стиль для компонента CircularProgress
 */
export const Circular = styled.div`
    display: flex;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
`
