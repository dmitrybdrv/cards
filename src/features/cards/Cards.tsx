import React, { FC } from 'react'
import { Root } from 'routes/root'

type PropsType = {}

export const Cards: FC<PropsType> = ({}) => {
    return (
        <div>
            <h1>Cards</h1>
        </div>
    )
}
